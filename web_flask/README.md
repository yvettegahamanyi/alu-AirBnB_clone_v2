# 0. Hello Flask!
A script that starts a Flask web application, your web application must be listening on 0.0.0.0, port 5000
and display 'Hello HBNB!' and use optin strict_slashes=False option in route definition

# 1. HBNB
A script that starts a Flask web application 
Your web application must be listening on 0.0.0.0, port 5000
Routes:
/: display “Hello HBNB!”
/hbnb: display “HBNB”
You must use the option strict_slashes=False in your route definition
